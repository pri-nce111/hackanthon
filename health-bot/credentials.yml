rest:
  # enabled by default at /webhooks/rest/webhook

rasa:
  url: "http://localhost:5005/api"

# Twilio (SMS/WhatsApp) - fill from .env
twilio:
  account_sid: "${TWILIO_ACCOUNT_SID}"
  auth_token: "${TWILIO_AUTH_TOKEN}"
  twilio_number: "${TWILIO_NUMBER}"
